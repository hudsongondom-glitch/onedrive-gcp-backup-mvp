$kind: http-request
name: GET Restore Backup
method: GET
url: "{{baseUrl}}/restore/:backupId"
order: 1000
description: Fetches backup details and returns download/restore info for a given backup. **Not yet implemented** — returns 501.
headers:
  x-session-id: "{{sessionId}}"
pathVariables:
  backupId: jkv8WbW4sLn2mjuo3gNM
